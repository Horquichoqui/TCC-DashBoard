{
  "name": "dashboard-pedagogico/root",
  "description": "Root composer for build management",
  "require": {
    "php": "^8.2"
  },
  "scripts": {
    "install-all": "composer install -d backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend && cp -r frontend/dist/* backend/public/",
    "serve": "php backend/artisan serve --host=0.0.0.0"
  }
}
